/** This library contains the types to represent 2D shapes generically.
 *
 * Note: This a duplication of EntityType.GeometryType and should be merged.
 */
export enum SHAPE3D_TYPE {
  LINE = 0,
  ARC = 1,
  CIRCLE = 2,
}
